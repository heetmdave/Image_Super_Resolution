


INSTRUCTIONS TO RUN BEST MODEL


required libs :torch torchvision numpy opencv-python (cv2) Pillow (PIL) tqdm glob os (built-in)


custom_img_path="input.jpg"

# make sure you replace the input.jpg & 
update the custom image path on opening the file in the first cell then run the subsequent
cells till you get this message  "TA DONT NEED TO RUN BELOW CELLS ELSE THE TRAINING MAY START"


==================================================================================


Model 2


# Libraries needed
os, torch, numpy, torchvision, PIL, tqdm, matplotlib, skimage

# Update image path in 247the line of final.py

For example:
input_image_path = "xyz.png"

# Run the script

python final.py

# Output will be generated in the same folder

final_sr_output_final.png



====================================================================================

Model 3


# Libraries needed
tensorflow, numpy, cv2, os, glob, skimage

# Place image inside the "inputImages" folder

# Run last cell of finalcode.ipynb file to generate output data

# Output will be generated in the specified folder in the last line of code

save_folder="OutputImage"


====================================================================================

Model 1



required libs :torch torchvision numpy opencv-python (cv2) Pillow (PIL) tqdm glob os (built-in)

Place the 800 HD images of div2k dataset inside DIV2K_train_HR folder. It is available at https://www.kaggle.com/datasets/joe1995/div2k-dataset

Place the 14 set14 images inside the set5_set14_data/Set14/Set14 folder. It is available at https://www.kaggle.com/datasets/ll01dm/set-5-14-super-resolution-dataset/versions/1

now run all cells step by step


====================================================================================











